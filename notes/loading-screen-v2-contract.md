Scope: Replace the current looping processing UI with a calmer, full-screen loading experience that feels premium, directional, and visibly alive without reading as recursive.
Constraints: Keep the existing stage flow and backend logic unchanged, preserve the MEDUSA visual language, and avoid loud repetitive motion.
Verification: Run `npm run build` after the UI change and confirm both processing stages render as full-screen branded states.
Exit Criteria: The loading view advances forward, avoids irritating loop behavior, and feels cinematic rather than stalled.
